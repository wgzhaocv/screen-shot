services:
  screenshot:
    build: .
    container_name: screenshot-service
    ports:
      - "4000:4000"
    volumes:
      - /Users/wenguang/Documents/server_uploads/screenshots:/screenshots
    environment:
      - NODE_ENV=production
      - SCREENSHOT_DIR=/screenshots
      - PORT=4000
      - CONCURRENCY=10
    restart: unless-stopped

